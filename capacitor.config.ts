import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Tugas_1_Metric_Converter',
  webDir: 'www'
};

export default config;
